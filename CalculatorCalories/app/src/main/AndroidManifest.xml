<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.main">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoadingActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegLogActivity" />
        <activity android:name=".RegistrationActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Colorie_Diary_Activity"
            android:label="@string/diaryLabel" />
        <activity
            android:name=".ChangeUserProprities"
            android:label="EditUserProfile" />
        <activity
            android:name=".ListProductsActivity"
            android:label="Product List" />
        <activity
            android:name=".EditUser"
            android:label="EditUserProfile" />
        <activity android:name=".InformationActivity" />
        <activity
            android:name=".CalendarActivity"
            android:label="Calendar" />
        <activity
            android:name=".CalendarDayActivity"
            android:label="Calendar" />
        <activity
            android:name=".CalculationsActivity"
            android:label="Calculations" />
        <activity
            android:name=".FormulesActivity"
            android:label="Formules" />
        <activity android:name=".InfoCalculations"></activity>
    </application>

</manifest>
